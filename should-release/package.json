{
  "name": "should-release-action",
  "version": "0.0.0",
  "private": true,
  "description": "Determines whether this release workflow run is needed, and cancels it if not",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "package": "ncc build --source-map --license licenses.txt",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "1.8.2",
    "@actions/glob": "0.3.0",
    "batch-runs-action": "*"
  }
}
