{
  "name": "batch-runs",
  "version": "0.0.0",
  "private": true,
  "description": "Cancels this workflow run if a newer one is pending",
  "scripts": {
    "build": "rimraf lib && tsc",
    "package": "rimraf dist && ncc build --source-map --license licenses.txt src/main.ts",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "1.9.1",
    "beachball-actions-common": "0.2.1"
  }
}
